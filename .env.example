# Copy this to `.env` and fill in. `.env` is gitignored.

# OpenAI API key — required if using gpt-* models
OPENAI_API_KEY=sk-proj-your_openai_key_here

# Gemini API key — optional. Leave empty to disable gemini-* models.
GEMINI_API_KEY=
